{
  "name": "crawlio-browser-guide",
  "version": "0.7.0",
  "description": "Companion CLI and read-only MCP server for Crawlio Browser Guide: install the native helper with one command, and let Claude Code or Codex see the browser page the user chose to share, with no way to act on it.",
  "license": "Apache-2.0",
  "author": "Rashid Azarang <rashid.azarang.eg@gmail.com>",
  "type": "module",
  "bin": {
    "crawlio-browser-guide": "bin/crawlio-browser-guide.js"
  },
  "files": [
    "NOTICE",
    "README.md",
    "bin",
    "src",
    "vendor"
  ],
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Crawlio-app/browser-guide.git",
    "directory": "mcp"
  },
  "homepage": "https://docs.crawlio.app/browser-guide/overview",
  "bugs": {
    "url": "https://github.com/Crawlio-app/browser-guide/issues"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "crawlio",
    "browser-guide",
    "chrome-extension",
    "native-messaging",
    "read-only",
    "accessibility-snapshot",
    "ai"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.0"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "prepublishOnly": "node -e \"import('./src/host-install.js').then(({isMachO, sha256, vendorMetadata}) => { const path = new URL('./vendor/macos/com.crawlio.browser_guide', import.meta.url).pathname; const meta = vendorMetadata(); if (!isMachO(path)) throw new Error('vendor host binary missing or invalid; run node ../scripts/package-mcp-host.mjs'); if (!meta || sha256(path) !== meta.sha256) throw new Error('vendor metadata is stale; rerun node ../scripts/package-mcp-host.mjs'); })\""
  }
}
